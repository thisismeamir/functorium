Can we feed a machine learning algorithm all the known primes and expect the next set of primes?

for example consider a training set which is based on the number of digits and the list of primes namely:

1 -> 2,3,5,7
2 -> ...
3 -> ...
Can it predict 4

Merely by the appearance of the numbers, it really doesn't check for the prime number to be true, just guess from how they look.

Second question is that can it be more accurate in other bases?

1 -> 10, 11, 101, 110 (base 2)

Can it be more predictive if the algorithm is written to have more and less mathematical functions in a prime number method? Letting the algorithm to add to its predictive function operations.